{
  "name": "@uios/adapters",
  "version": "1.0.0",
  "type": "module",
  "description": "UIOS Plugin Architecture for UI Library Adapters (shadcn, Magic UI, 21st.dev, Aceternity, Origin UI)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test test/**/*.test.ts"
  },
  "dependencies": {
    "@uios/compiler": "file:../compiler"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
